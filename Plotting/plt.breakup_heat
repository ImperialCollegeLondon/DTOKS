set xlabel "Time (s)" font "Helvetica,20"
set ylabel "Temperature (K)" font "Helvetica,20"
set xtics font "Helvetica,20"
set ytics font "Helvetica,20"

set key off
set grid
filename(n) = sprintf("Data/breakup_hm_%d.txt", n)
plot for [i=0:50] filename(i) using 1:2 with line lw 3

